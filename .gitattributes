# From https://help.github.com/en/articles/dealing-with-line-endings#per-repository-settings

# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Declare files that will always have LF line endings on checkout. (as per editorconfig)
* text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
